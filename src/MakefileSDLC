CC = gcc
CFLAGS = `idris --include` `pkg-config --cflags sdl2`

sdlrun.o: config sdlrun.c sdlrun.h

config: .PHONY
	echo "%flag C \"`pkg-config --libs sdl2`\"" > Graphics/Config.idr
	echo "%flag C \"`pkg-config --cflags sdl2`\"" >> Graphics/Config.idr

clean: .PHONY
	rm sdlrun.o

.PHONY:
